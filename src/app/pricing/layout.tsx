export const metadata = {
	title: "SoundWave | Pricing",
	description: "Our pricing plans are designed to fit your needs.",
}

export default function PricingLayout({
	children,
}: {
  children: React.ReactNode
}) {
	return (
		<>
			{children}
		</>
	)
}